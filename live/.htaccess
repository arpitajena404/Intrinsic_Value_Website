# Disable parent directory rewrite rules from interfering with this subdomain
RewriteEngine On

# Redirect /admin to the new admin URL
RewriteRule ^admin/?$ https://live.intrinsicvalueequity.in/invest_biz/admin.html [R=301,L]

# Ensure index.html is served as the default page
DirectoryIndex index.html
